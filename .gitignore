node_modules
*.local
.DS_Store
.vscode/*
!.vscode/extensions.json

# dist/ is deliberately NOT ignored. Consumers install this package straight
# from git, so the built output has to be committed for `npm install` to work
# without a build step. Use `npm run release` — it rebuilds before tagging.
